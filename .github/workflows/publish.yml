name: Publish Master
on: 
  push:
    branches:
    - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Build the Docker image
      run: docker build . -t lgohr/sonarqube:${{ github.sha }} -t lgohr/sonarqube:latest
    - name: Login to Registry
      run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
    - name: Push to Registry
      run: |
        docker push lgohr/sonarqube:${{ github.sha }}
        docker push lgohr/sonarqube:latest
    - name: Logout
